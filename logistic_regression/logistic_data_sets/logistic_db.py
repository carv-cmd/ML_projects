import sqlite3 as sql3

if __name__ == "__main__":
    """ 
    Use to create a database schemas and pull data from an external files
    Tests database connection when 'logistic_db.py' is ran explicitly 
    As well as running a test on queries to said database
    """
    try:
        pass
    except ValueError:
        pass

else:
    """ 
    Once database is created or connection is established 
    Call from another module to access the data object for use
    """
    try:
        pass
    except ValueError:
        pass
